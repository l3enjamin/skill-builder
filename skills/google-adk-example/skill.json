{
  "name": "google-adk-example",
  "description": "Example skill that uses Google ADK to perform a web search via an AI agent.",
  "metadata": {
    "keywords": ["google", "adk", "agent", "search", "gemini"],
    "version": "1.0.0"
  },
  "execute": {
    "input": "query: string (The question to ask the agent)",
    "process": [
      "Ensure GOOGLE_API_KEY is set in your environment (export GOOGLE_API_KEY=...).",
      "Run the agent: terminal.execute('bun skills/google-adk-example/src/index.ts \"<your_query>\"')"
    ]
  }
}
